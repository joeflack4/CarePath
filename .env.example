# CarePath AI Environment Configuration
# Copy this file to .env and update with your actual values

# ======================
# service_db_api
# ======================
MONGODB_URI_LOCAL=mongodb://localhost:27017
MONGODB_URI=mongodb://localhost:27017
MONGODB_URI_CLOUD=
#MONGODB_URI=
MONGODB_DB_NAME=carepath
API_PORT_DB_API=8001
LOG_LEVEL=INFO

# ======================
# service_chat
# ======================
CHAT_API_PORT=8002
DB_API_BASE_URL=http://localhost:8001

# LLM settings
LLM_MODE=mock
# LLM_MODE=Qwen3-4B-Thinking-2507  # Uncomment to use Qwen model (requires model download)

# Optional: Hugging Face token for model download (if model repo requires auth)
# HUGGINGFACE_TOKEN=your_token_here

# Optional: Custom model cache directory
# MODEL_CACHE_DIR=/app/models

# Vector DB settings
VECTOR_MODE=mock
# VECTOR_MODE=pinecone  # Uncomment to use Pinecone (not yet wired in MVP)

# Pinecone settings (for future use)
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=carepath-documents

# Deploy
# - 1 var covers all cloud deployments: demo, test, prod, etc.
## AWS
### DEPLOY_AWS_PROFILE: An SSO profile
DEPLOY_AWS_REGION=
# DEPLOY_AWS_PROFILE: i thought it was genonaut-admin, but `cat ~/.aws/config` shows `[profile AdministratorAccess-513291423126]`
DEPLOY_AWS_PROFILE=
# # this is the number that comes at the end of `DEPLOY_AWS_PROFILE`. Can also verify this by doing: aws sts get-caller-identity --profile ${DEPLOY_AWS_PROFILE}
DEPLOY_AWS_ACCOUNT_ID=
DEPLOY_AWS_RDBMS_SERVICE_ROOT_PW=
## Terraform
DEPLOY_TF_STATE_BUCKET_NAME=
DEPLOY_TF_DYNAMO_DB_TABLE=